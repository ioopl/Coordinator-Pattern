import UIKit

// MARK: - Models (Domain)

// Keep simple for now; can be expanded later (e.g., from a repository, API, etc.)
struct LoginModel {
    let loginButtonTitle = "Log In"
    let cancelButtonTitle = "Cancel"
}
